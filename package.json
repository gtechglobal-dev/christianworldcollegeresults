{
  "name": "christian-world-college-result-checker",
  "private": true,
  "scripts": {
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npm ci && npx prisma generate --schema=prisma/schema.prisma",
    "build:frontend": "cd frontend && npm ci && npm run build",
    "postinstall": "cd backend && npm ci"
  }
}
